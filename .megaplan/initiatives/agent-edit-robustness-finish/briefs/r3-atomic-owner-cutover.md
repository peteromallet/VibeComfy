# R3 — Atomic Native Owner Cutover and Decisive Incident Proof

## Outcome

Consume the complete R2 readiness handoff to route every native graph consumer
through the adapter, delete all competing owners and forward replacement paths,
close all S3/S4 rows, and prove the exact incidents forward and backward in one
indivisible production release boundary. This closes M2.

## Input handoff

- R2 certified cutover manifest bound to the current base SHA.
- R2 detached zero-debt ledger rehearsal, fault harness, and exact real-fixture
  reproduction evidence.
- R1 accepted private executor and M1 versioned contracts.

R3 must stop before editing if any R2 handoff is missing, stale, ambiguous, or
does not reconcile with current source.

## IN

- Publish only the receipt-based preflight, structural/layout apply, inverse,
  and authorized compensation surface defined by the certified manifest.
- Route every enumerated preview, Apply, capture, rollback, Undo, recovery,
  replay, picker, scope, and overlay native consumer.
- Delete every enumerated native mutation closure, fallback, copied stable-index/
  link helper, forward graph loading path, dead import/export, and compatibility
  facade in the same change.
- Regenerate the source-derived ledger: 27/27 coupled rows transferred, seven S4
  debts eliminated, deleted tuples absent, and scanner exact both directions.
- Add poison gates proving forward Apply cannot call clear/configure/loadGraphData;
  compensation is receipt-authorized and unreachable forward.
- Prove full `a66422e…` and `eb45e…` prepare → Apply → native serialize →
  verify → finalize → reload/persist, plus meaningful partial failures, inverse,
  compensation, rollback, and tab-switch zero writes in real ComfyUI.
- Resolve semantic fields such as `width`, `height`, `ckpt_name`, and
  `filename_prefix` through native widget identity. Layout-owned getter-only
  node properties must never be treated as logical workflow fields or written.
- Emit typed physical-carrier evidence for semantic writes: logical field,
  native widget identity, resolved native/serialized index, before/after value,
  and landed step. Never derive a serialized widget index from the ordinal of
  `inputs[*].widget`; auxiliary widgets may be serialized without descriptors.
- Make the transition from side-effect-free preflight to the first native write
  explicit in adapter evidence. A preflight failure has an empty landed prefix
  and cannot authorize inverse mutation or whole-graph compensation.

## OUT

Verifier ownership, controller/API extraction, exhaustive recovery UX, full
compatibility/lifecycle matrix, terminal repository audit, and nested scopes.

## Locked decisions

- Routing, old-owner deletion, ledger flip, and decisive incident proof are one
  indivisible release boundary; no partial state may merge.
- Adapter emits evidence, not semantic verdicts.
- `candidateGraph` never reaches native mutation; whole-graph loading is
  compensation-only; titles never identify groups.
- R2 handoff is mandatory and source-drift-sensitive.

## Open questions for the planner

- Only discrepancies discovered while reconciling R2 against current source.
  Resolve by regenerating R2 evidence or stop; do not invent a mid-cut design.
- Minimal authorized Undo seam required for R6 without implementing recovery here.

## Constraints

- No feature flag, wrapper, alias, broad scanner allowlist, or half-owner commit.
- Use full fixtures and actual ComfyUI serialization.
- Preserve M0/M1/scheduler gates and protected files.

## Done criteria

- Certified manifest is consumed completely with no unhandled item.
- Production ledger is source-derived, scanner-exact, 27/27 transferred, and
  zero open S3/S4/migration-debt rows.
- Forbidden old definitions/imports/exports/calls/fallbacks are absent.
- Exact incidents pass forward finalize/reload and failure/inverse/compensation/
  rollback in real ComfyUI.
- The getter-only width fixture proves semantic widget mutation without an
  arbitrary JavaScript node-property write.
- The KSampler fixture proves `control_after_generate` can occupy a serialized
  widget position absent from `inputs`, while `denoise` lands on its named native
  carrier and the adapter receipt records the exact physical resolution.
- Focused adapter/ownership and all broad browser, roundtrip, Python, and parity
  gates pass; two independent acceptances find no partial owner or mock proof.

## Touchpoints

Adapter, roundtrip, `comfy_adapter.js`, picker/replay/scope/overlay consumers,
ledger/scanner, ownership tests, real-ComfyUI specs, and incident fixtures.

## Anti-scope

No partial merge, compatibility facade, allowlist escape, reduced fixture,
self-attestation, M4–R8 responsibility absorption, or protected-file change.

## Output handoff and proof artifacts

- Atomic cut SHA, complete deletion audit, and regenerated zero-debt ledger.
- Decisive real-ComfyUI incident evidence with receipts, landed prefixes,
  projections, serialization/reload, inverse, and restoration proof.
- Stable sole-owner adapter evidence contract consumed by R4.

## Megaplan dial

Overall plan difficulty: **5/5**; profile: **partnered-5**; robustness:
**thorough**; depth: **xhigh**; vendor: **claude** with active GLM 5.2; prep
required. A partial authority migration can pass local tests while corrupting
non-local graph behavior.

Prep direction: validate every R2 artifact against current source and freeze the
indivisible diff; stop on any stale or unresolved readiness evidence.
