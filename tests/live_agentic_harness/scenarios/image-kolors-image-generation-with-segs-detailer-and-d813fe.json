{
  "id": "image-kolors-image-generation-with-segs-detailer-and-d813fe",
  "query": "Replace the Ultralytics detector with GroundingDINO for mask generation, and adjust the pipeline so that SAM segmentation still receives valid SEGS input from the new detector.",
  "workflow_path": "external_workflows/corpus/d813fedaabea87b7.json",
  "network": true,
  "timeout": 260,
  "apply": false,
  "assessment": {
    "expect_graph_changed": false,
    "allow_safe_refusal_outcome_kinds": ["requires_custom_nodes"],
    "expected_no_candidate_reason": "GroundingDINO exists neither in this corpus workflow (detection is UltralyticsDetectorProvider + SAMLoader + Impact SEGS detectors) nor anywhere in the authoritative object_info schema — exact and declared-family-prefix lookups return no rows — so no graph edit can swap the detector. The query deliberately demands an absent class; the typed terminal for a proven named-schema absence is a requires_custom_nodes refusal, not a generic clarify.",
    "expected_no_candidate_absent_classes": ["GroundingDINO"]
  },
  "desired": {
    "outcome": "The agent inspects the detection chain, determines that no GroundingDINO class is authorable in this environment, and truthfully reports that no candidate change exists; the graph stays unchanged.",
    "quality": "The refusal names the detectors actually present (UltralyticsDetectorProvider, SAMLoader, Impact SEGS) and gives the grounded absence reason; no fabricated edit is submitted and existing nodes remain intact.",
    "alternatives_ok": true
  },
  "_tags": {
    "modality": "image",
    "query_type": "big_adjustment",
    "abstraction": "med",
    "complexity": "high",
    "manifest_complexity": 4,
    "task_type": "text_to_image",
    "requires_custom_nodes": true,
    "techniques": [
      "kolors",
      "image-generation",
      "segs-detailer",
      "inpainting",
      "sam",
      "ultralytics",
      "chatglm3",
      "text-to-image"
    ],
    "source_workflow_id": "d813fedaabea87b7",
    "source": "external_workflows/corpus",
    "staged": true,
    "authored_by": "deepseek-v4-flash",
    "author_rationale": "The query deliberately demands GroundingDINO, a detector class absent from both this corpus workflow (detection is UltralyticsDetectorProvider/SAMLoader/Impact SEGS detectors) and the authoritative object_info index; adjudication requires the authoritative named-class blocker to cite it before any leg passes. The scenario's honest expected outcome is therefore a grounded expected-no-candidate requires_custom_nodes refusal rather than the detector swap the query asks for."
  }
}
