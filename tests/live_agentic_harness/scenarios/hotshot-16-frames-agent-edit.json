{
  "id": "hotshot-16-frames-agent-edit",
  "query": "Switch to generating 16 frames with Hotshot",
  "workflow_path": "tests/fixtures/agent_edit/hotshot_base_unsaved_workflow_4.json",
  "apply": false,
  "network": true,
  "timeout": 300,
  "assessment": {
    "expect_graph_changed": false,
    "allow_safe_refusal_outcome_kinds": ["clarify", "requires_custom_nodes"],
    "expected_no_candidate_reason": "The fixture graph is a plain SDXL image-to-image pipeline (CheckpointLoaderSimple/KSampler/VAEEncode) with no frame-count widget and no video output, and no Hotshot generation class exists anywhere in the local authoring schema. The query deliberately demands an absent tool; a correct run reports no candidate instead of fabricating an edit."
  },
  "desired": {
    "outcome": "The agent inspects the graph, determines that Hotshot video generation and any frame-count control are absent from both the graph and the local schema, and truthfully reports that no candidate change exists; the graph stays unchanged.",
    "quality": "The refusal gives the grounded reason (SDXL image pipeline, no Hotshot classes or frame widgets available to author), proposes no fabricated edit, and leaves existing nodes intact.",
    "alternatives_ok": true
  }
}
