# Route-intent map

Run the canonical VibeComfy executor entrypoint for four representative user
asks and verify that each resolves to the correct canonical route:

- "make it more cinematic" -> clarify (no research, no implementation)
- "what does this workflow do?" -> inspect (no implementation, graph explained)
- "change the seed to 42" -> revise (local graph edit)
- "add the Wan control LoRA chain" -> adapt (research precedent then edit)

Freeze the resulting executor envelopes as evidence and assert the route,
research/implement flags, and Apply eligibility match the four-route taxonomy.
