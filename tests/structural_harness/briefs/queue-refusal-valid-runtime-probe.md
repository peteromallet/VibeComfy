# Gate the queue on runtime evidence

Decide whether this graph may be queued against the runtime. A bare claim that
the runtime schema tier was consulted is not proof. Only a fresh, independently
verifiable runtime probe receipt may satisfy the queue gate; attach it through
the queue validation stage handoff.
