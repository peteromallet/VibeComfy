To switch to SDXL, I need to first check the exact signature of the SDXL text‑encoding node so I can replace the current `CLIPTextEncode` nodes correctly. Let me look that up now.
```batch
search(focus_types=["CLIPTextEncodeSDXL"])
```