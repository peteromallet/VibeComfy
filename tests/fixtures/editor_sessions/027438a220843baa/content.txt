I'll add a new SaveImage node after the VAE decode and wire the decoded images to it.
```batch
saveimage_2 = SaveImage(images=vaedecode.image, filename_prefix='canonical_test', near=vaedecode)
done()
```
