# Ground-truth resolutions (23 scouts) — input for Codex B solidification

Repo: /Users/peteromalley/Documents/reigh-workspace/vibecomfy. These correct and refine Codex A's 65-task design. Apply them when solidifying the final task list.

S1 routes: 4 module-level wrappers are dead (prepare/finalize/rollback/reconcile_turn_transaction — aiohttp handlers shadow them with local imports). 4 functional handlers test-only (_handle_agent_edit, _handle_agent_edit_accept, _handle_agent_edit_chat, _handle_agent_edit_rebaseline, _handle_agent_edit_audit — no /agent-edit/audit route). reject_turn wrapper ALIVE. Production /agent-edit/* submit routes go through _handle_agent_executor_submit (executor.core.run_executor), NOT legacy _handle_agent_edit → the two are behaviorally distinct; legacy chain test-only.
S2 edit.__all__: MEMBERSHIP-ONLY, order NOT contractual (derived from globals() at import). Split may reorder freely as long as every frozen name stays importable from the edit façade.
S3 monkeypatch surface: 13 names patched via monkeypatch.setattr('...edit.<name>'): _ws_send, _stage_validate, _stage_emit, _stage_summarize, _stage_convert, _stage_audit, _stage_agent_batch_repl, run_agent_turn, run_agent_turn_batch, run_agent_turn_delta, queue_stage_result, run_model_turn, load_agent_generated_scratchpad. Plus 4 imported-by-name: handle_agent_edit, _format_batch_report, _format_batch_report_json, _stamped_turn_response_outcome. All must remain top-level edit-module attributes after extraction.
S4 batch deps: batch REPL CANNOT use normal imports after extraction — 80 external names, 75 real deps (58 private + 17 public) spanning 19 modules, and many host modules are themselves exec'd fragments (private helpers not importable as module attrs) → REQUIRE a dependency object (or same exec-into-namespace mechanism). Task T-035 must specify the dependency object design.
S5 session surface: PLAN STALE. session __all__ = 23 entries (20 imported by name; IdempotencyConflict/IdempotencyReplay/TurnAllocation never imported). Real totals: 29 public attrs (20 __all__ + 9 non-__all__: REVIEWABLE_CANDIDATE_STATES, STRUCTURAL_PROJECTION_VERSION, DEFAULT_LOCK_TIMEOUT_SECONDS, LOCK_FILE_NAME, LOCK_LEASE_SECONDS, load_candidate_transaction, load_candidate_transaction_with_migration, project_transaction_state, structural_graph_hash, structural_graph_projection, iter_turn_records) + 23 private _-prefixed helpers imported by name (incl. _transaction_receipts_for_turn, _scoped_sentinel_payload, _validate_v2_transition, _write_response_immutable). Executor/core.py, executor/execution_plan_builder.py, commands/_agent_edit_debug.py are additional importers. Manifest must use these exact lists.
S6 write_state_atomic: PLAN STALE. _artifact_store ranges (:1147-1459, :3467-3647) contain ZERO write_state_atomic call sites → no late-binding needed for _artifact_store. The ONE extraction-range caller is @6398 in _turn_state_machine (:5828-6402) → late-bind write_state_atomic there if extracting. Only one test monkeypatches it (backend_spine:10044, targeting record_idempotent_response which stays in session.py).
S7 runtime: TWO owners + TWO timeout contracts: session.py _comfy_server_argv/_spawn_comfy_server (ServerSession path; raw TimeoutError('Managed Comfy server did not become ready within N seconds'), default 300s, NO next_action) vs config.py/server_process.py (comfy_server ctx; RuntimeStartupError with next_action 'Check the ComfyUI startup log...', hardcoded 120s). Tests pin the session path argv via fake_server. Task list must pick ONE owner + ONE exception shape explicitly (recommend: session's richer argv + RuntimeStartupError with next_action + configurable 300s).
S8 deep_plain: MUST implement Family-A manual recursive semantics (map/entries; pass primitives/null/undefined/functions/symbol values through; drop symbol keys; discard prototypes; do NOT preserve repeated refs). Add explicit WeakSet cycle guard (decide throw vs return-original deliberately — both families currently mishandle cycles; JSON family aliases on cycle = bug). Family-B (JSON) call sites must be audited for undefined/fn loss. NO JS tests exist for any clone helper today — add them.
S9 golden fixtures: PLAN STALE. Golden fixtures (layout_operation/m1_projection/mutation_materialization golden_v1) are parity corpora for Python+JS tests, NOT generator I/O. Generator inputs = contracts.py constants (REBASELINE_RECOVERY_FIELDS 8 fields, PUBLIC_OUTCOME_KINDS, INTERNAL_TO_PUBLIC_OUTCOME, FAILURE_HINT_KEYS) + 2 embedded blocks; output = agent_edit_response_contract_generated.js only. Remove the fixture→generator task edge.
S10 generated JS: test-only import; Python emits corrupted_delta (ops.py:53-57, edit_response_contract.py:603) but generated JS + generator hard-code delta_corrupted (divergent spellings, latent mismatch). Fix: align (map or rename) — decide canonical spelling.
S11 roundtrip clone: clonePlainData is already JSON round-trip; preserve post-clone method injection in cloneDynamicSlot (serialize method, :1523-1541) and liveLinkRecord (:1660-1693) — re-cloning those breaks the in_i/out_i naming contract. All other callers pass pre-serialized data.
S12 weakmaps: PLAN STALE — TWO WeakMaps (submitActivityByPanel:416, pendingTransactionSnapshotByPanel:420) + ONE plain object submitWatchdogDepsState:410 (NOT a WeakMap; scalar test-injected deps). Task text must say two keyed WeakMaps + one scalar deps object.
S13 preview cache: must NOT move to WeakMap — keyed by primitives (candidateGraphHash string etc.); lifecycle-owned-by-clear contract; forcing object keys breaks ownership map + 5 consumers. Keep on panel.state.
S14 chat rehydrate: lifecycle cannot wholly own without absorbing normalizeChatRehydratePayload (snake/camel alias normalization, roundtrip:5514) + normalizeFieldChangesFromMessage (:5753). Either move both into lifecycle/contract owner, or keep the split (decide).
S15 schema forks: safe to delete — zero external importers of schema/{local,object_info,parsing,runtime}.py.
S16 runtime/config.py: safe to delete after repointing runtime/server.py:9 + server_process.py:13 to runtime.session.
S17 make gap: NO gap metric exists. 347 test_*.py files; make check covers 20 (~5.8%). pytest-xdist NOT declared (absent from pyproject [dev] + uv.lock) — the -n 8 runs were ad-hoc. full-pytest target does NOT exist. T-016 must declare pytest-xdist in [dev] and add the target; document the real 5.8% coverage gap.
S18 demo_factory: python -m only (no console script). Legacy invocations + exit codes to preserve: python -m vibecomfy.demo_factory (→cli), -m vibecomfy.demo_factory.cli (run-creative via scripts), scripts/run_one_additive.py <i> <base> (exit 0/2), run_campaign.main() (0 if pass-rate≥0.5 else 1), cli.py exit 1 on missing-arg/no-campaign/failed-export; run-case/run-creative return 0.
S19 stale docs: exact replacements — contracts.md: agent_session.py→session.py (×5), [agent_edit.py]→[edit_ingest.py] (for _stale_rebaseline_recovery_issue) and →[edit_response_contract.py] (for _failure_response), [agent_contracts.py]→[contracts.py], agent_gates.py→gates.py. wire-protocol.md: agent_provider.py→agent/provider.py (×2), agent_contracts.py→contracts.py. response-contract.md: agent_contracts.py→contracts.py (×2). exec_node_design.md (design draft, correct only if touched): edit_apply.py→porting/edit/apply_links.py + apply_resolve_add.py, edit_session.py→porting/edit/_parse.py + _resolve.py, agent_provider.py:210-254→agent/provider.py, emitter.py:4846→porting/emit/signatures.py. d02 (historical): node_packs_install.py→node_packs/_install.py, read_lockfile in _lockfile.py, pack_resolver.py→registry/pack_resolver.py (version_pin exists).
S20 LTX2_NAG: slot four = 'inplace' (static WIDGET_SCHEMA has None placeholder; consume.py curates index 3 → 'inplace'; NOT apply_to_all; not version-dependent in name).
S21 DiagnosticLike: Protocol surface = code/message/severity/detail ONLY; must NOT require to_json; must NOT be runtime_checkable; SHOULD be publicly exported from ir/__init__.py. Keep domain classes separate; do NOT touch commands/_diagnostics.py Diagnostic.
S22 web_dist: .gitignore alone NOT sufficient — hatch wheel target has NO exclude for vibecomfy/comfy_nodes/web_dist (packages=['vibecomfy'] + artifacts); generated dist can leak into the wheel. Add [tool.hatch.build.targets.wheel.exclude] + sdist excludes. Runtime resolution (__init__.py WEB_DIRECTORY) is safe.
S23 xdist+quarantine: quarantine gate works under -n 8 (controller-side terminal summary; workers read-only). gpu excluded by default ('not gpu'); live/runpod/runpod_full flag-gated. pytest-xdist must be declared in [dev] (see S17).

## Codex B mission
Produce the FINAL solidified task list (T-001…, linear, execute-and-review) integrating: (1) Codex A's 65-task design (structure/lanes/oracles), (2) ALL 23 resolutions above (correct the stale tasks, add new tasks for discovered issues: delta_corrupted spelling alignment, hatch wheel exclude, pytest-xdist declaration, deep_plain cycle-guard semantics, batch dependency-object design, runtime exception-shape decision, session surface exact lists), (3) the weak-worker protocol, (4) oracle checkpoints with exact briefings. Output the complete final task list — every task: id, title, files, change, verify command, risk/size, deps, lane, oracle. Include a short 'resolutions applied' note per changed task. This IS the executable artifact.
