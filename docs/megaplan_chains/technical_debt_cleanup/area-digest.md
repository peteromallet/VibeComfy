# 28-Area Deep-Dive Digest (Codex input for detailed cleanup plan)

Repo: /Users/peteromalley/Documents/reigh-workspace/vibecomfy (all green: make check exit 0, full pytest exit 0). Each area: verdict, key evidence, recommendation.

1. Apply authority — apply_core.py is the single canonical apply_delta; apply.py is a zero-logic re-export; EditSession + batch loop DRIVE it op-by-op; authority_receipts.py is the replay/proof layer. No redundancy. Cleanup: unify cosmetic import split ('from .apply' vs 'from ...apply_core').
2. Batch REPL — batch execution REUSES EditSession.apply_batch → apply_delta. Single apply engine. The three edit_batch_loop_* files are fragments of ONE _stage_agent_batch_repl function stitched by the exec-assembler (see 19).
3. Agent turn execution — provider.py = canonical orchestration facade; runtime.py = real backend; fixture_provider.py = deterministic e2e backend. By-design provider-seam; add a TurnBackend Protocol; do not merge.
4. Agent session routes — clean 3-way boundary: session.py owns persistence+lifecycle, routes.py pure HTTP adapter (has some dead sync handlers), edit_state.py is a source-chunk of edit.py. No lifecycle duplication. Cleanup: remove dead sync handlers in routes.py.
5. Runtime configuration — TWO fully competing SessionConfig classes: runtime/session.py (canonical, adds strict_drift + richer argv) vs runtime/config.py (stale 9-field mirror, only importers runtime/server.py:9 + server_process.py:13). Delete config.py + repoint the two imports.
6. Runtime server startup — TWO _spawn_comfy_server: runtime/server_process.py (canonical, used by server.py) vs runtime/session.py:1371 (richer: sage-attention flags, 300s readiness, RuntimeStartupError; used ONLY by ServerSession). Consolidate onto server_process upgraded with session's semantics; delete session.py:1328-1433, keep _comfy_server_argv as re-export (CLI + tests patch it).
7. Hash/JSON normalization — THREE semantic families: A = _canonical_contract_primitives.py (agent-edit authority; session.py + candidate_transaction.py are facades); B = porting/reorganise/orchestrate.py independent (_freeze_jsonish/_canonical_bytes sort_keys=True); C = web/canonical_hash.js mirror (docstring WRONG: cites orchestrate.py but behavior matches _canonical_contract_primitives). Fix docstring; keep families, document boundaries.
8. Python/JS contract ownership — 3 patterns: (A) closed-op mirrors (layout_operation_v1, mutation_materialization_v1, projection_registry_v1) — by-design, golden fixtures under tests/fixtures/agent_edit/ are the parity gate (pytest + node --test on SAME fixtures); (B) canonical_delta.js one-way mirror of porting/edit/ops.py; (C) generated (agent_edit_response_contract_generated.js ← tools/generate_agent_contract_js.py). Recommend: promote golden fixtures to declarative source; GENERATE pure constant/rule tables into JS from Python (existing generator pattern); keep validation control-flow hand-mirrored; keep dual-consumer golden-digest CI gate. Fix canonical_hash.js docstring.
9. Projection registry — projection_registry_v1.{py,js} = canonical authority pair; graph_projection.js/field_registry_v1.js/identity_contract_v1.js = pure re-export facades (keep); porting projections + preview_diff_core.js = read-only consumers (keep). Split is the design; no consolidation.
10. Graph/identity models — ONE identity contract owned by projection_registry_v1.{py,js}; uid.py/scope.py single leaf; ledger at porting/edit/ledger.py (not comfy_nodes/agent/). Facades justified. Mirror is the drift risk; keep parity gate.
11. Workflow loading — ingest/normalize.py = canonical engine; cli_loader.load_workflow_any = public gateway; ONE genuine duplication: cli_loader.py:33-37 .json branch == registry/library.py workflow_from_file — delegate. workflow_source.py = justified adapter. Two documented back-compat aliases.
12. Schema providers — schema/provider.py = canonical monolith; schema/{local,object_info,parsing,runtime}.py = DEAD parallel fork (self-referential, zero external importers, stale copies). DELETE the four.
13. Node-schema resolution — canonical = schema/provider.py (SchemaProvider protocol, schema_for); _compile/_widgets.py = canonical widget-name source; everything else (porting/widgets/*, object_info/consume.py, edit/_resolve.py, call_validation.py) are layered consumers/shim with distinct roles. Keep layering; note object_info/consume.py maintains its OWN curated widget orders (dup risk).
14. Error envelopes — TWO disconnected universes: framework VibeComfyError (errors.py, CLI) vs agent-turn FailureEnvelope (contracts.py, canonical wire). PRIMARY redundancy: routes.py has TWO parallel HTTP envelope builders (functional _handle_agent_edit + aiohttp register_agent_edit_routes async handlers re-implementing classify_failure/_json_error). Also two contracts.py (agent vs executor) same name disjoint content; next_action concept duplicated. Fix: unify routes.py builders; decide VibeComfyError vs FailureEnvelope boundary.
15. Diagnostic types — 8 classes NOT structurally redundant (each has domain fields), but {code,message,severity,detail}+to_json copy-pasted ~7x. Canonical base ir/diagnostic.py::Diagnostic exists but underused (only ValidationIssue inherits; stale docstring claims ContractIssue/NodeCallValidationIssue inherit). Recommend: extract structural Protocol OR have dataclass forms inherit plain Diagnostic base; leave DiagnosticRecord (durable record) alone.
16. Clone/freeze utils — 6 JS copies split into TWO divergent groups: Group A recursive structural clone (canonical: agent_edit_lifecycle.js:44 etc.) vs Group B JSON round-trip (agentic_replay.js:109, preview_picker.js:170, roundtrip.js:2780 — DROPS undefined/functions, aliases on cycles = real bug at agentic_replay.js:532-568). freezePlainData ×2 identical. Create web/deep_plain.js shared module; migrate all 6 (audit Group B call sites for undefined/cycle dependence); leave .mjs deepFreeze trio.
17. Preview/replay/commit — ONE reducer: transition() in agent_edit_lifecycle.js:413; agent_lifecycle_commit.js thin facade; preview_picker/agentic_replay commit via helpers. Split is the design; no work.
18. Conversation/memory stores — one owner per durable concept: session.py turn artifacts (transcript authority), chat.json = justified projection (NOT canonical), edit_state.py batch_turns carrier, executor/research.py producer, journal_durable_v1 {py,js} by-design mirror. No consolidation needed.
19. Exec-assembler split — edit.py exec-assembles 18 fragments into one namespace; __all__ = ALL globals (incl. private helpers) — the flat name surface IS the public contract (routes.py + 12+ test files import from it). Split MUST preserve the flat name surface via re-exports. Found a DEAD duplicate: _canonical_delta_ops_envelope_payload in edit_transform_stages.py is silently overwritten by edit_response_contract.py's identical def. Migration sequence: freeze the export surface (already = __all__), then mechanical split with re-export shims.
20. session.py decomposition — orchestration monolith, but projections/hashing/candidate/migration already delegated to sibling modules. Extract 3 modules: _artifact_store (:1147-1459, :3467-3647), _turn_state_machine (:5828-6402), _v2_scoped_validation (:4637-5821). Keep session.py thin façade; preserve __all__ + module-attr surface (12+ importer sites, tests patch write_state_atomic / import private helpers).
21. Lazy facades/cycles — contracts/__init__.py + agent/__init__.py lazy maps = start-up-cost deferrals, NOT cycle-breakers (submodules acyclic vs parent). emitter.py has NO lazy imports (eager re-export). TWO genuine cycle-breakers MUST stay lazy: function-scoped guarded `from vibecomfy.comfy_nodes import NODE_CLASS_MAPPINGS` in agent/diagnostics.py:140 and edit_transform_stages.py:786. Safe: make contracts' light names eager; keep heavy deferrals; keep the two guarded imports.
22. Frontend monolith — roundtrip.js (13k) CAN be split along frontend_ownership_map.md: reducer already centralized; shell is orchestrator. Dead: renderAudit/renderDebug (zero callers). Misplaced: exec-node normalization (map assigns to comfy_adapter). Double projection: chat rehydrate (shell normalizeChatRehydratePayload + lifecycle ingestChatRehydratePayload). Extract: agent_submit_flow.js (~9100-10330), agent_apply_flow.js (~10330-12000), agent_rebaseline_undo.js, agent_turn_reducer.js, preview-diff cache; move intent/exec normalization to comfy_adapter; delete dead renderers; collapse chat rehydrate; keep 3 WeakMaps in one injected-deps module.
23. Generated node monsters — nodes/core.py (32,926) + kjnodes.py (9,144) are 100% GENERATOR-OWNED (tools/generate_node_shims.py; header marker; .pyi stubs; object_info cache = source of truth). Do NOT split by size (regeneration overwrites whole modules; splitting breaks nodes/__init__.py MODULES + importers). Correct move: per-file regeneration freshness gate (tools/commands/_checks.py already scans stubs).
24. Durable stores/locks — 4 layers: agent sessions (session.py SessionStateLock — authoritative cross-process mutex), runtime sessions (liveness registry, no lock), runs (mkdir-exist_ok allocation), browser storage. journal_durable_v1 dual-validated. No consolidation; keep as-is.
25. Pack/node resolution — remote (registry/pack_resolver.py resolve_missing_nodes/resolve_pack) vs local (node_packs/_defs.py from lockfile) = layered, keep. Lockfile owner = _lockfile.py. Fix: tools/class_inventory_audit.py:44-48 hand-parses lockfile TOML (duplicate of read_lockfile — format-drift risk) → use read_lockfile. Stale doc: node_resolution_epic d02 claims no version pin (now supported).
26. CLI surfaces — canonical CLI = vibecomfy/cli/ + commands/ registry (26 commands, singular, not duplicated). Two installed products (vibecomfy, vibecomfy-agent). REAL cleanup target: demo_factory has THREE overlapping campaign entry surfaces (click cli.py, run_campaign.main(), scripts/run_one_additive.py wrapper) → pick one canonical + thin wrappers. scripts/vibecomfy_debug.py = documented compat shim (keep).
27. Test-harness duplication — browser-contracts (15 files) is a STRICT SUBSET of browser-smoke (both run in make check → those 15 run twice; both pure-Node, no Playwright) → drop browser-contracts from check or fold. strict-ready's 9 pytest files ⊂ fast's 19 (double-run; unique value only in check_strict_ready_templates --json) → dedupe. ~180 test_*.py referenced by NO make target = CI coverage gap (not dup). Python<->JS parity pairs are by-design (keep). Quarantine = by-design-red attribution (keep).
28. Deletion/config audit — DELETE: vibecomfy/testing/agent_edit.py (zero importers), schema/{local,object_info,parsing,runtime}.py (dead fork), runtime/_local_library_yaml.py (acquire_library_yaml never called; session.py reads extra_model_paths.yaml directly). RETAIN/REGENERATE: agent_edit_response_contract_generated.js (drift-guarded); web_dist (gitignored build artifact, absent on disk — regenerate not delete). DELETE: .desloppify/*.bak|.lock. UPDATE not delete: stale docs paths (agent_provider/agent_edit/agent_contracts → agent/*).

## Cross-cutting priorities (from verdicts)
- High-value, low-risk: DELETE the 6 dead modules (12, 28); dedupe make targets (27); demo_factory CLI consolidation (26); delete routes.py dead sync handlers + unify the two envelope builders (4, 14); fix canonical_hash.js docstring + class_inventory_audit TOML parse (7, 25).
- Medium: deep_plain.js shared util + Group B bug fix (16); protocol/base for diagnostics (15); schema provider widget-order dup (13); exec-assembler split with surface freeze (19); session.py 3-module extraction (20); roundtrip.js carve (22); runtime config/server consolidation (5, 6).
- By-design, leave alone: apply layering (1, 2), provider seam (3), projection registry (9), identity (10), preview/replay (17), memory stores (18), generated node shims (23), durable stores (24), pack resolution split (25), lazy facades (21 — only fix docstrings).
