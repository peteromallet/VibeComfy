# Copy-paste handover message

Please receive the unified VibeComfy canonical workflow integrity + clean editable Python Megado plan.

Project repository: https://github.com/peteromallet/VibeComfy.git

Source SHA: `20975e25f0401b64f51dfeef5d1250cfbd97600f`

Handover ref: `handover/unified-workflow-integrity-20260909`

Package: `docs/handover/unified-workflow-integrity-20260909/`

Inspected skills repository: https://github.com/peteromallet/poms-skills.git

Exact skills SHA: `ef42515942adfb1683cde4b7b2d53d4e56dbe25e`

Clone into a new directory (do not overwrite an existing checkout):

```sh
git clone --single-branch --branch handover/unified-workflow-integrity-20260909 https://github.com/peteromallet/VibeComfy.git VibeComfy-unified-workflow
cd VibeComfy-unified-workflow
git rev-parse HEAD
git cat-file -e 20975e25f0401b64f51dfeef5d1250cfbd97600f^{commit}
```

Start with `docs/handover/unified-workflow-integrity-20260909/START-HERE.md`. Read the pinned Megado skill using dependencies.md's no-overwrite fetch or direct-read fallback, then goal.md, northstar.md, run.yaml, status.md, consolidation.md, decisions/emission-D1.md, plan.md, tasklist.md, acceptance-ledger.md and execution-start.md. Verify manifest.json and that imports/commands use this checkout.

State: consolidated and ready to execute, but receiving mode remains `planning_only`; no product implementation has been completed. Fresh H3 onboarding on the source SHA failed closed on class/schema errors and produced no new candidate; the existing main Python is still messy. Baseline evidence is included and must not be called E2 acceptance. No dirty patch is needed: all relevant sender changes are already in the selected source.

Objective: one shared onboarding/emission path gives clean editable Python, meaningful handle wiring, no routine IDs or native-port payloads in ordinary calls, no duplicate runtime values or replay/data dump tail, and correct subgraph/interface/identity/provenance behavior. Preserve atomic save/reload/export, draft handling and revision fences. Programmatic source-shape, semantic and failure tests plus the actual whole-file H3 edit/rebuild/regeneration test are required; I1–I10 are currently unproven. The latest Astra sense check also requires real CLI/SDK/canvas ingress parity including malformed-boundary refusal, structural/presentation assertions plus recorded inspection of the exact exported H3 graph and whole generated Python file. Read evidence/astra-end-state-sense-check.md; its two required corrections are already adopted in the acceptance ledger and task briefs.

Honor the role bindings in run.yaml: the designated host remains coordinator/adjudicator, delegate normal work, and retain E1's existing XHARD route. Verify exact model/reasoning availability and native delegation; no silent substitutions. Stages/caps come only from run.yaml: save_export_completion (2 rounds maximum), then final (3). Preserved spent counters: reviews 6/11, oracle 6/7; remaining 5 review calls and 1 oracle call. The user explicitly added and spent one Astra planning sense check; the original five execution calls remain. Do not import extra review allowances from the absorbed plan.

Prerequisites: Python >=3.11, declared project dependencies/pytest and a YAML reader, hard command timeouts, and configured agent access. Preserve the existing no-install/no-GPU/provider/server/model-download limits and <=1 GiB output limit; missing prerequisites must be reported. No fabricated pass or arbitrary requirement weakening.

Finish instruction for this handover: verify receipt and report readiness or exact missing prerequisites. Do not start product implementation until explicit delivery instruction. On that instruction, change mode in the authoritative run.yaml, preserve counters and criteria, establish C0 in an isolated implementation checkout, then execute the dependency-ordered plan. No PR, implementation push, merge, deployment or production cutover is granted by this handover; obtain an explicit finish instruction if one is later requested.
