# Authority and scope

The user explicitly requested that Pythonic emission and canonical integrity be bundled into this canonical plan, that the obsolete duplicate be retired after preserving its unique content, and that current-main H3 onboarding be run and opened. Those preparation actions are complete. The user then invoked Megado handover with its public-publication default; the selected destination is the existing public VibeComfy origin, on the handover branch only.

Preparer authority: package, sanitize, verify, commit and normally push only this selected handover directory on its dedicated branch. Preserve other worktrees/source. No force push, main merge or deployment.

Receiving mode remains planning_only; no explicit answer changing it was received during packaging. The recipient may inspect/verify the handover and report readiness. Product implementation and its tests begin only on explicit delivery instruction, preserving run.yaml roles/stages/caps and status counters. Packaging does not grant a PR or implementation-push finish; no merge/deployment/GPU/provider/runtime provisioning is inferred.

The user requires clean whole-file Python, not just syntactic validity or hiding a replay tail in metadata. I1–I10 and the actual H3 practical acceptance remain binding. No additional model review gate or budget is created by this handover.
