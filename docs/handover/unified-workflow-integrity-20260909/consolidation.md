# Consolidation

The user made this run the canonical destination for save/export integrity and editable Python. The old Pythonic plan is absorbed, not declared complete or silently discarded.

## Absorption map

| Absorbed work | Unified owner | Treatment |
|---|---|---|
| D1 shared emitter/projection/custody ruling | E1; I8/I9 | Adopt as prior decision; no second architecture review. |
| A1–A6 baseline | E0/T0; I8/I9 | Reuse audits and existing fixtures; add missing AST/edit/custody evidence. |
| Coupled emitter/identity kernel | E1 → T1 | One Sol XHARD implementation; publication consumes it. |
| Regression migration | T3; Q6/Q5 | Shared behavior and custody proof. |
| H3 regeneration/practical test | E2/T4; I10/I6 | Exact saved source, normal path, edits, rebuild, deterministic comparison, truthful docs. |

Natural behavioral and identity/provenance fidelity supersedes importer-shaped exact syntax. Unexpected loss remains an atomic refusal. Resolver-owned helpers may lower only with preserved authored provenance; unknown custom nodes remain fail-closed raw calls.

## Unified dependencies and estimate

`C0 → (T0 and E0 independently) → E1 → T1 → T2`, with T3 fixture preparation allowed after C0 and integration after E1 and the invariants it consumes; then the shared completion checkpoint, `E2 → T4 → final`. E1 is the only emitter implementation. The unified estimate is 7–10 ordinary focused engineer-days plus 1–2 contingency, lower than summing the plans because they share the emitter/publication seam, fixtures, regression evidence, and H3/docs handoff.

## Boundaries

No second emitter, pretty-only mode, generic custody framework, global ingest migration, schema-provider redesign, forced persistence lifecycle, deployment, merge, push, runtime provisioning, or GPU/model work is included.
