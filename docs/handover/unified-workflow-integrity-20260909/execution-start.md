# Start the unified integrity and Pythonic-emission delivery

Prepared 2026-09-09. Read goal.md, northstar.md, run.yaml, tasklist.md and status.md first. The current mode is planning_only: these assignments are prepared, not dispatched for product implementation. On an instruction to execute this plan, record delivery authorization and switch mode to delivery without resetting counters. Read the installed Megado execution.md reference before source mutation. Host holds coordinator and oracle responsibility; normal assignments resolve through worker_normal in run.yaml. No merge/push/deployment is implied.

## Source and C0 startup

Run from the receiving repository root and read [provenance.md](provenance.md) and [dependencies.md](dependencies.md). Set PROJECT_ROOT to `git rev-parse --show-toplevel` once. SOURCE_WORKTREE is the clean clone at the pinned handover ref. BASE_SHA is `20975e25f0401b64f51dfeef5d1250cfbd97600f`; no dirty source patch is needed. Source investigation verified all relevant local edits are already in that commit. Preserve any receiving checkout changes and do not reset/stash/clean them.

When product delivery is explicitly authorized, copy this package into a new run control directory `${PROJECT_ROOT}/.otto/runs/canonical-integrity-plan-20260908` using a no-overwrite check; preserve run.yaml/status counters. Set OTTO_DIR to that directory and WORKTREE to a new isolated delivery checkout created through the pinned Megado execution.md mechanics. Do not overwrite an existing run/worktree; resume only after verifying its identity. Keep source code edits out of the handover branch.

C0 outcome: actual isolated candidate identity, environment/dependency identity, temporary/output root and capacity; normal route through worker_normal. Evidence: base/candidate SHA/tree or snapshot, clean status or owned dirty manifest, chosen environment, output limits and no original overwrite. Recheck changed refs if execution occurs later. The former dirty-overlay uncertainty is resolved: do not require the sender's machine or reapply an old handover patch. Return to the host oracle only for consequential source/semantic conflict, not ordinary setup.

Use Python >=3.11 with declared project dependencies already available and the timeout command (or the executor's equivalent hard wall-clock limit). Verify `vibecomfy.__file__` resolves into this checkout; do not silently test a different installed version. No package/schema/model/runtime installation is authorized by this planning-only handover. Runtime limits remain 300/600/900 seconds and <=1 GiB temporary output.

## Task briefs

Each dispatch includes northstar.md verbatim, current goal, its task row and I criteria verbatim, the consolidation amendment and adopted emission D1, actual source SHA/tree or snapshot manifest, working directory, dependency state, output paths and the following compact assignment. Ordinary tasks route normal → worker_normal; E1 alone routes xhard → worker_xhard with the adopted D1 justification. No worker may widen scope. A precise test failure with an in-scope fix is authorized corrective work; uncertainty over architecture/scope returns to the host. Each worker reports actual command/result/evidence, not only a narrative PASS.

### T0 — one preservation fixture

Outcome: resolve one representative custom/opaque payload through existing preservation or explicit atomic refusal, while retaining current PreviewAny/auxiliary-output preservation and wired passthrough behavior in the H3 source (PreviewAny is not UI-only furniture). Dependency: C0. Scope: existing emitter fixtures/raw metadata owners. Evidence: Q0 plus one fixture covering UID, widgets, raw properties and edges; identify expected normalized content or explicit refusal. At most 0.5 focused engineer-day. Return if the fixture requires a general policy/new representation; do not launch broad custom-node discovery.

### T1 — exact save identity and atomic refusal

Outcome: prove current preparation/publication behavior and repair demonstrated I1/I2 gaps. Dependency: C0/T0/E1 for integrated proof; fixtures may prepare earlier. Scope: emit_prepare.py, workflow_bundle.py and focused bundle/emitter tests; inspect current H3 changes first. Evidence: Q1, exact returned/reloaded semantics and UID assertions, canonical Python/.vibe.json bindings/revision, and injected node/edge/widget loss leaving BOTH previous files unchanged, including an existing-sidecar case. Draft persistence must not call queue readiness. No digest-drift allowance or generic normalization framework. Serialize mutations with T2 in shared owners.

### T2 — public export integrity

Outcome: prove existing CLI export and repair demonstrated I3 gaps using existing checks. Dependency: C0 for independent fixture preparation; T1 coherent candidate for shared changes. Scope: _export.py and new tests/test_canonical_export_integrity.py, plus a narrowly required shared owner. Evidence: Q2 through the real public command route; all named I3 scenarios, API/recovery/trust assertions, output and no-write directory comparisons. Explicit --out must not silently persist layout state; --persist-sidecar updates .layout.json. This is distinct from T1 canonical Python/.vibe.json publication. Preserve draft export, breadcrumb layout, strict/visible force-drop and preview behavior; no forced canonical lifecycle.

### T3 — semantic parity and retained fencing

Outcome: test Python/typed one-field 20→30 parity, untouched UIDs, no-op and rejected-edit/history behavior; repair only demonstrated mismatches. Dependency: C0 for independent fixture preparation; integrated proof consumes E1 and relevant T1/T2 invariants. Scope: existing session/parse/typed tools and reconciled dirty edit gate dependencies. Evidence: Q3 and relevant Q5 revision/runtime cases, positive semantics and negative atomic rollback/stale-revision assertions. Preserve both approved transports; mocks prove contract behavior, not live GPU execution.

### T4 — docs and final integration

Outcome: named docs/help match tested draft/save/export/apply/queue and provenance/revision behavior. Dependency: E2 and T1–T3 settled and save_export_completion gate respected for affected dependents. Scope: named tasklist docs, CLI help and integrated candidate. Evidence: Q4 manual source/help-to-test mapping for I6; Q5 once on final integrated candidate, exact candidate/environment receipts for all I IDs. Fix affected failures then rerun affected checks only. No unrelated documentation redesign.

## Tests, review and corrections

Run Q0–Q5 from the isolated WORKTREE using the explicit interpreter and timeout paths in tasklist.md. Q2 and Q5 depend on the new export test module existing. Proposed commands have not been executed by preparation. Use temporary deterministic fixtures with <=1 GiB output; 300/600/900-second ceilings remain. Missing baseline dependencies are an environment issue: diagnose within existing tools; do not silently install or weaken proof.

The coordinator owns integration and the broad Q5 invocation. Preserve coherent current dependency state in every worker brief. S1_READY and final are the only review triggers. At S1_READY, freeze C0/T0/E0/E1/T1/T2 candidate/evidence and invoke reviewer_normal once; stop early on PASS. Final uses final_reviewer on the whole integrated outcome. Read run.yaml for caps, status.md for spent counters. No extra review is hidden in these startup instructions.

For every first-pass or correction dispatch, record outcome, acceptance evidence, source/dependency scope and normal/xhard route. XHARD needs the irreducible-question justification and uses the configured slot. After two failed substantive corrections, request root-cause adjudication rather than repeat. Before actual reviewer/oracle invocation debit the existing counter; failures/lost responses remain charged. Current D5 and inherited emission D1 are already charged; the direct user consolidation amendment is not another model decision invocation. Reviewers remain independent leaves.

A native receipt records task/agent path, actual model/reasoning and tool, cwd and source identity, exact brief and North Star hashes, result artifact/hash and completion state. Record timestamps/PID/process exit only if exposed; otherwise null with explanation. Freeze actual review candidates/packets under execution.md mechanics, without pretending a planning manifest is implementation proof.

## Absorbed emission assignments

### E0 — normal baseline

Outcome/evidence/dependencies: use the E0 task row and I8/I9 verbatim. After C0, add meaningful AST and edit/rebuild fixtures to existing emission suites; run Q6, preserving concrete failures as baseline. Include helper lowering, public/default edits and identity/native-port custody. Do not migrate assertions by deleting preservation checks; pair behavioral assertions with custody evidence. Normal → worker_normal.

### E1 — XHARD coupled kernel

Outcome/evidence/dependencies: use the E1 task row, I8/I9 and decisions/emission-D1.md verbatim. After E0/T0, implement one shared projection/emission/finalization contract with topological handle kwargs, one runtime-value authority and compact custody. Existing IDs/public bindings/native ports must survive without routine per-call IDs or replay. Q6 and affected T1 proof must pass before the shared stage. XHARD → worker_xhard (Sol): nonlocal semantic and identity rebinding can pass syntax/API smoke checks while breaking variants, native ports or edit authority; independent cosmetic changes cannot settle it. Return on a demonstrated inability to preserve the adopted D1 contract; no silent fallback or second emitter.

### E2 — normal exact H3 practical proof

Outcome/evidence/dependencies: use E2/I10 verbatim. After shared stage/T3, regenerate the archived source JSON using normal CLI, edit the actual generated source/public controls, rebuild through load_bundle, and compare values/effective wiring/output/custody plus deterministic regeneration. Include prompt, source passage, video/audio masks, duration and seed. Preserve exact source and artifact hashes and open the result. No manual cleanup, copied interim builder, schema provisioning or GPU claim. Normal → worker_normal. The separately requested current-main onboarding is a baseline; it cannot close E2 against a future redesigned candidate.

## Clean-source acceptance clarification — user inspection request

I8/I9 apply to the whole generated authoring file, not just build(). Moving the replay tail into a large CUSTODY dictionary is not acceptance. The one deterministic custody declaration contains only bindings/IDs and necessary schema/provenance/exceptional reference facts; it contains no runtime input/widget values, whole-node snapshots, graph edge arrays, raw UI payload or serialized source graph. Required native-port facts must not be repeated in ordinary calls and again in custody. Generated source has no post-construction wf.nodes[...] input/widget/metadata/native-field replay; the existing internal finalization mechanism may apply verified custody without generating such a tail.

Meaningful prompt, model, source, mask and interval controls are permitted as named values. A JSON-valued node input is not itself a defect; repeating it in constructor literals and later restoration is. Each runtime value has one authoritative expression, shared by references where reused. E0/E1 AST and declaration-content assertions prove this distinction; E2/I10 opens the actual whole H3 file and rejects merely relocating the old clutter. No arbitrary line/byte limit or new serialization/storage service is introduced. D1's return condition remains if necessary custody cannot be preserved under this contract.

## Handover source resolution — 2026-09-09

The current source closure check found all 12 relevant dirty edit/revision paths and six untracked tests byte-identical to origin/main 20975e25. No source patch is required. The only differing tracked converter file removes an existing diagnostic and is not adopted. See provenance.md. This resolves the earlier dirty-overlay uncertainty; the recipient starts from the pinned committed source, checks its own custody/environment in C0, and need not recover the original machine's checkout. Original local bytes remain untouched. Pythonic requirements remain included; omitting the obsolete source patch does not omit that work.

## Latest Astra sense-check requirements

Apply the two mandatory evidence clarifications in [the acceptance ledger](acceptance-ledger.md#astra-sense-check-acceptance-clarification--2026-09-09): E0 maps the existing ingress owners and establishes cross-entrypoint baseline; E1 corrects only demonstrated shared-owner/emission defects; T3 adds the focused regressions to Q5; E2 saves/reloads and exports the edited H3 graph, asserts presentation/subgraph structure and records inspection of the exact rendered graph and whole Python file. A nested-definition source edit is a recommended optional test choice within existing I8/I9. No added task family or review stage.
