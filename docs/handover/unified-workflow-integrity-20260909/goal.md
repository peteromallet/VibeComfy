> Current authority: **delivery approved**. Execute the plan and required tests now. Earlier planning-only statements below are historical and superseded.

# Goal — one canonical onboarding path with clean editable Python

Produce natural, editable Python from supported source workflows through the shared canonical onboarding/emission path, then preserve effective behavior and required identity/provenance through edit, save/reload, export and approved execution transports. The whole file must be readable: no loose IDs in ordinary calls, raw-ID topology, duplicated runtime values, replay tail or serialized graph dump disguised as custody. Necessary prompt/mask controls remain single named values. Subgraphs, interfaces and provenance must be represented correctly or explicitly refused; no universal unsupported-graph promise.

Direction: [northstar.md](northstar.md). Full required scenarios: [acceptance-ledger.md](acceptance-ledger.md). Tasks and tests: [tasklist.md](tasklist.md). [run.yaml](run.yaml) is the single role/model/stage/budget declaration; [status.md](status.md) preserves spent counters. The host explicitly holds coordinator and oracle responsibilities; normal work is delegated, E1 retains the adopted XHARD binding. No automatic model substitution.

Source is public main `20975e25f0401b64f51dfeef5d1250cfbd97600f`; [provenance.md](provenance.md) resolves the prior dirty-source uncertainty. This package is the portable form of unified canonical-integrity-plan-20260908 v5. Both original workstreams are included; the obsolete separate Pythonic plan was retired after preserving its unique material. [D1](decisions/emission-D1.md) remains the binding emission design; [consolidation.md](consolidation.md) maps its tasks and criteria.

Receiving mode remains planning_only until explicit execution authority. The user requested a handover, not immediate implementation or a PR finish. Preparer may publish this handover branch only. No product code implementation, PR, merge, push of implementation, deployment, runtime provisioning, provider/GPU calls or model downloads are authorized by packaging. Later explicit delivery authority changes mode in the single run.yaml without resetting counters; finish instruction is to return outcome evidence and blockers, unless the user then supplies a different finish instruction.

All I1–I10 implementation evidence is NOT RUN/MISSING. Existing H3 native support is a prerequisite, not proof of the later natural-Python contract. The [main baseline](evidence/h3-main-baseline.md) actually ran but refused fresh conversion; it is not successful E2 or final review evidence. No future executable review packet exists yet; assemble it from the real integrated candidate and required test results.

Use the existing environment and deterministic offline fixtures. No dependency/schema/runtime installs, server/provider/GPU work or model downloads under current limits; <=1 GiB local output. Focused checks <=300s, affected checks <=600s, final matrix <=900s. Missing capabilities or dependencies are explicit prerequisites; do not invent evidence or silently weaken requirements. Full plan execution remains 7–10 focused engineer-days plus 1–2 contingency, not agent elapsed time.

## Latest user authorization — execute

The user instructed: “And give them approval to execute too, keep it simple.” This supersedes earlier planning-only/start-permission instructions throughout this package. Receiving mode is delivery: begin C0, implement the unified plan, fix in-scope defects and run all required programmatic and visual acceptance checks. Preserve roles, scope, review/oracle counters and resource boundaries. Report completion with evidence and any actual unresolved blockers. No additional permission to start is required.
