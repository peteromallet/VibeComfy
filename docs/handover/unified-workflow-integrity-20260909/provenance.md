# Portable source and publication provenance

Project: VibeComfy. Destination repository: https://github.com/peteromallet/VibeComfy.git (public). Handover branch: `handover/unified-workflow-integrity-20260909`. Base branch: `main`. Selected source commit: `20975e25f0401b64f51dfeef5d1250cfbd97600f`. Origin/main was fetched and verified at that SHA. The sender's older local-main checkout was left unchanged.

The portable package derives from unified canonical-integrity-plan-20260908 v5, including the user's correction that Pythonic emission and integrity are one outcome. It does not clone the sender's ignored control directory or claim its implementation is complete.

## Source closure

The relevant source checkout was at `792b3ff7f809ee66f812881fb6b84f4c4030679b` with uncommitted work. All 12 selected edit/revision files and six untracked tests were compared by Git blob identity with `20975e25f0401b64f51dfeef5d1250cfbd97600f` and matched. No source patch is needed. The sole differing tracked converter version would omit committed stderr guidance and was not adopted. No original dirty files were changed or discarded.

The selected source already contains H3/native expansion and boundary diagnostics. It does not satisfy the later D1 natural-Python contract. Old handover source patches are omitted to avoid replaying already-landed changes. Required Pythonic scope is fully included in E0/E1/E2 and I8/I9/I10; the full adopted decision is [emission-D1.md](decisions/emission-D1.md).

## Selected artifact closure and omissions

Include full current plan, task/test contracts, criteria, direction, goal, run configuration, status/counters, adopted D1, scope authorization, review template, H3 source JSON and sanitized baseline summary. The package manifest records every intended file. Original H3 SHA-256: `2dd64fe26c42281962e434841c458cc935b1d1858e83093b882bbaeb02dc3121`; upstream provenance is in [the baseline](evidence/h3-main-baseline.md).

Omit raw agent transcripts, personal paths, caches, watchlists, old source patches, archived duplicate plans, unrelated dirty work and full raw command receipts. The original broader source-run history is retained at the sender; its earlier 21/26 accepted-task and 35-rework history is not reset, transferred as new review allowance, or treated as acceptance of the stricter current contract. The current counters and surviving decisions needed to execute are in status.md and decisions/.

## Dependency identity

Inspected Megado and handover skills commit: `ef42515942adfb1683cde4b7b2d53d4e56dbe25e` at https://github.com/peteromallet/poms-skills.git. The relevant committed files and six direct-read URLs were verified byte-for-byte. See dependencies.md for no-overwrite fetch and fallback reads.

## Publication audit and limits

The intended package is scanned for credentials, private operational details, personal/home paths and excluded artifacts. H3 strings are public workflow/model metadata. The source base is already on this same public destination: its 1,760 reachable commits were inspected with bounded static pattern searches. They contain inherited example credentials, security records and local-path references; no history rewrite or new publication of unrelated local history is performed. Static pattern checks do not prove every historical match is inert. Only this selected handover directory is added to the already-public base; no logs/caches/private source overlays are staged.

Fresh-checkout verification checks manifest, portable links, source/ref identity, YAML roles/stages/counters, complete criteria, H3 hash and pinned dependency reads. This is packaging verification, not product-test or model-review PASS. Actual future candidate review packets must be assembled after implementation and required tests.

## User-requested Astra sense check

One independent native `gpt-6-astra` high planning review examined the previously published `ed0d16d2a4b86d3e2750b1349a2a4e189110cbe8` package. Its two in-scope acceptance-evidence corrections were adopted without product execution. Latest user authority adds/spends one planning-review call, preserving five future execution calls and the oracle budget. This publication changes plan artifacts only.

## Main publication

The user subsequently requested direct main publication to simplify receipt. The complete plan package and Astra corrections were integrated onto latest origin/main `623e7f6a386193927e32d23dd475df12cc9bc193`, preserving its intervening changes. Main is now the receiving ref; the earlier handover branch is historical. The inspected product baseline remains the source SHA above; C0 must check current source before implementation. Only plan artifacts changed. Local unrelated work remains untouched.
