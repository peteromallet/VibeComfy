# Preserved planning history and counters

The objective and source audit originated before this handover. No product implementation is certified by these planning notes. The original audit cited twelve explorers; six additional conversational Luna investigations refined it before the named planning run. They are inherited factual provenance, not delivery review calls.

The named run completed five planning model-review invocations: two Luna settled critics in one wave, one Luna contract review, one Astra final plan judgment and one Astra v2 delta judgment. Four oracle interactions occurred: initial direction, discovery synthesis, final disposition and v2 disposition. The last two were mixed review/oracle calls under the previous process and remain conservatively charged to both budgets; they are not four distinct model calls.

| Stable decision | Preserved ruling | Portable record |
|---|---|---|
| D1 | Normalization precedes bundle/sidecar/revision; identity-only acceptance insufficient; preserve source work | decisions/oracle-initial.md |
| D2 | Reuse existing owner, no generic result framework, serialize shared writes, exact public-export proof | decisions/discovery-rulings.md |
| D3 | Bounded plan accepted, normal work, no justified XHARD | decisions/oracle-final.md |
| D4 | Two review boundaries retained; effort calibrated; bounded remaining question | decisions/oracle-v2.md |

Local planning corrections and subsequent user-directed simplifications did not erase these invocations. Raw receipts, transcripts, archives and machine paths are deliberately not published. Their relevant decisions/counters are retained here; absence of raw logs is not evidence of a fresh review allowance. Intermediate/final executable stage consumption remains zero.

Historical worker receipt groups covered exploration, planning and revisions; same-scope correction followups also occurred. They are not reviewer or oracle calls. Later history/portability fact checks are packaging work and cannot issue hidden product verdicts. Model invocation timing/process details not exposed by native tools are not invented.

The user-directed v3 configuration uses lifetime ceilings rather than resetting counters: existing five reviews plus the declared future stage allowances; existing four oracle interactions plus the configured future decision allowance. See run.yaml for exact numbers. Preserved passing planning judgments never replace actual implementation evidence.
