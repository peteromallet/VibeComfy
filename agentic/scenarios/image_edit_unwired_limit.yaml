name: image-edit-unwired-limit
tier: 2
description: "Limits: agent recognizes image.edit is not a wired verb and names the escape hatch without attempting it"
mode: structural
agents:
  - id: builder
    dispatcher: fake
tags:
  - m2
  - discovery
  - limits
  - structural
assessment:
  enforced:
    - "actions.jsonl contains forbidden_call_absence with forbidden_call_absent=image.edit and status=confirmed."
    - "actions.jsonl contains escape_hatch_note referencing the ready edit template (edit/qwen_image_edit)."
  graded:
    - "No compiled_api.json exists (agent did not attempt to compile the unwired verb)."
  observed:
    - "report.md names the ready edit template as the escape hatch."
extras:
  required_frozen_evidence:
    - evidence/actions.jsonl
