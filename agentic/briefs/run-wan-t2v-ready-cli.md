# Run wan_t2v

Run the `video/wan_t2v` template exactly as-is from the CLI.
Do it through the ready-template path rather than rewriting the workflow in Python, and tell me where the video would land.
