# Broken Graph Diagnosis

A video workflow won't compile. Something in the graph is disconnected — an input on one of the nodes has no upstream source feeding it. Figure out which node is broken and which input field is orphaned. Then describe how you would fix it.
