# Edit Request

Edit an image for me with `image.edit(...)` so it looks like a polished sci-fi poster.
If that exact helper does not exist, switch to the nearest supported workflow path and say what the escape hatch is.
