# Scoped Edit Verification

I tweaked one setting in a video template — the random seed on the sampler node. Prove that nothing else in the graph changed. Compute a compile diff and confirm the change is isolated to exactly that one input field.
