I'll read the full contract set and the attempt-2 plan fixes first, then spot-check HEAD against the claims.I’ll spot-check HEAD against the compose-map claims and the two attempt-1 fixes (r3 fail-closed, persist merge).**1. Persist path still retargets runtime index rows (check 2 + 4).**  
A3 now uses `build_cache(..., full_pack_refresh=False)` and drops higher-tier classes, then no-ops if none remain (`plan.md:62–64`). That only covers the all-covered case. HEAD `serialize.py:327–333` merge **re-adds every same-pack class** from existing files; `:229–230` then sets `index[class_type] =` the **new** `{pack}@{version}.json`. C extracts the whole pack (`only_classes` default `None`, `extract.py:742`; C 3.3 never passes it). Mixed pack (runtime class R + gap G) therefore copies R into `Pack@on_demand_*` and repoints `index[R]` — silent demotion. The runtime **file** stays on disk. Checkpoint A still only requires “no-op; runtime file unchanged” (`plan.md:85`), not that `index.json` still maps R to `@runpod-snapshot`. A/C/E empty-tmp tests will not catch this.

Attempt-1 item (1) is fixed: C 3.3 does not pass `allow_embedded`; `--no-embedded` is a documented no-op; sync is A→C→D→E.

**Required before freeze:** persist must update index keys **only** for newly written on-demand classes; Checkpoint A must assert a mixed-pack case leaves the runtime index row on the runtime file.
