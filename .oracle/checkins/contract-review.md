I'll read the full run contract and related findings first, then spot-check the repo against the plan's claims.I'll spot-check the plan's line numbers, compose-map symbols, and whether deferred Batch B is still required in-path.**1. C still requires B’s API (check 3).** C’s header defers B / r3 fail-closed, but C task 3.3 still calls `extract_pack_schemas(..., allow_embedded=not args.no_embedded)` (`plan.md:148`). HEAD `extract.py:737-746` has no such kwarg — B task 3 adds it. C task 1 also ships `--comfy-version` / `--no-embedded` with embedded default-ON (`plan.md:140-141`). Literal C either TypeErrors or lands B.

**2. `full_pack_refresh` can demote runtime captures (check 2).** A task 3 mandates `build_cache(..., full_pack_refresh={pack_slug})` (`plan.md:62`). `serialize.py:221-228` + `325-326` then drops other same-pack classes from the pack file **and** `index.json`. Task 5 / Checkpoint A only require the runtime *file* unchanged (`plan.md:84`), not the index mapping. C extracts the whole pack (`only_classes` default `None`, `extract.py:742`). Plan can retarget `@runpod-snapshot` index rows to on-demand — silent lower-tier substitution. A/C/E checkpoints use empty tmp caches and will not catch this.

Fix before freeze: (1) C must not pass `allow_embedded` / must default r3 skipped until B; (2) persist with merge (`full_pack_refresh=False`) and skip higher-tier classes; Checkpoint A must assert the index still points at the runtime file.
