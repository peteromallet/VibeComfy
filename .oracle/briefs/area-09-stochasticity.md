Explore area: Single-run stochasticity — which scenarios are flaky?

Context: the final measurement is one 100-scenario lane; model variance can flip individual scenarios between runs (G0 flip run already showed scenario-level differences run-to-run). The report must distinguish regressions from model variance.

Task: cross-reference scenario results across available runs (out/agentic/live-final, live-tail9, live-g0, live-postfix, live-flash, June baseline if present): which scenario IDs flip pass/fail between runs, which are stable pass, which stable fail. Report verified per-scenario stability classes (with counts + IDs), a suggested small flaky set for targeted repeats, unknowns, risks, and how the final report should present variance. Ranked findings, <300 words.
