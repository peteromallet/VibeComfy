`HEAD d2975269` — READ-ONLY audit:

| # | Criterion | Verdict | Evidence `path:line` | Receipt quote |
|---|---|---|---|---|
| 1 | Empty cache → preflight FAIL + `vibecomfy schemas ensure --manifest <path>` | **PASS** | `tests/test_batch_e_e2e.py:148-152` `with pytest.raises(ScenarioObligationError)` + `assert "vibecomfy schemas ensure --manifest {manifest}" in msg` ; `vibecomfy/schema/ensure_capture.py:8` `format_schema_gap` ; `.oracle/plan.md:235` | `.oracle/receipts/batch-E-execution.log:37` `Empty tmp cache → preflight_scenario_obligations fails with vibecomfy schemas ensure --manifest <m>` |
| 2 | `ensure --manifest` mocked registry + REAL extract on git fixture `FixtureNode.INPUT_TYPES` not `@stub.json` | **PASS** | `tests/test_batch_e_e2e.py:30-44` `_FIXTURE_PACK_SOURCE` `class FixtureNode: INPUT_TYPES` ; `:49-64` `_make_fixture_clone` `git init` + `nodes.py` ; `:67` `_patch_registry` `PackRef(url=FixtureURL)` ; `:164-175` `_cmd_schemas_ensure` + `_sandbox_provider` `OnDemandInstallSchemaProvider` + `glob fixture-pack@on_demand_*-{sha7}.json` ; `.oracle/plan.md:150-153` | `execution.log:36` `Local git repo custom_nodes/fixture-pack/nodes.py with FixtureNode.INPUT_TYPES, cloned via OnDemandInstallSchemaProvider LRU sandbox, extracted via real extract_pack_schemas (method ast/import)` |
| 3 | After ensure preflight GREEN `resolution_tiers[FixtureNode].source_kind∈{on_demand_static,on_demand_import}` | **PASS** | `tests/test_batch_e_e2e.py:179-196` `entry[source_kind] in (on_demand_static,on_demand_import)` → `preflight` `result[ok]==True` + `tier[source_kind]==source_kind` `tier[extraction_rung]==rung` ; `.oracle/plan.md:190-195` | `execution.log:38` `preflight green with resolution_tiers[FixtureNode]={source_kind:on_demand_*, extraction_rung, locked_commit}` |
| 4 | `runtime_only=True` / `VIBECOMFY_OBLIGATION_RUNTIME_ONLY=1` rejects after on-demand | **PASS** | `tests/test_batch_e_e2e.py:198-205` `preflight(manifest, runtime_only=True)` raises + `monkeypatch.setenv(VIBECOMFY_OBLIGATION_RUNTIME_ONLY,1)` raises ; `.oracle/plan.md:201-204` | `execution.log:38` `runtime_only=True and VIBECOMFY_OBLIGATION_RUNTIME_ONLY=1 correctly reject` |
| 5 | `@stub.json` never passes as live | **PASS** | `tests/test_batch_e_e2e.py:207-214` `SomePack@stub.json` → `ObjectInfoIndexSchemaProvider.get_schema("Stubbed") is None` ; `.oracle/plan.md:244` | `execution.log:38` `@stub.json filtered` |
| 6 | Evidence matrix exists & matches test (command, source_kind, commit, rung, preflight, strict, stub) | **PASS** | `.oracle/evidence/batch-E-matrix.md:3-9` row `vibecomfy schemas ensure --manifest <comparison.json>` \| `on_demand_static\|on_demand_import` \| `git rev-parse HEAD {sha7}` \| `ast\|import` \| `empty→fail / after→green` \| `runtime_only reject` \| `@stub never` ; `tests/test_batch_e_e2e.py:7` docstring link | `.oracle/checkins/batch-E.md:29` `Real FixtureNode git fixture, mocked registry, real ladder, honest on_demand_* + pin, strict reject, @stub filtered` |
| 7 | Receipts ran `tests/test_batch_e_e2e.py` and it passed — conflict? | **PASS** — no conflict | `tests/test_batch_e_e2e.py` 4 tests | `execution.log:21` `python -m pytest tests/test_batch_e_e2e.py -q` → `.... 4 passed` ; `.oracle/findings/batch-E-verify/batch-E-verify-tests.txt:18` `4 passed, 1 warning in 1.58s` ; `.oracle/checkins/batch-E.md:5` `tests/test_batch_e_e2e.py → 4 passed, 1 warning in 1.08s` — consistent |

**Final: E2E-PASS** — all 7 claims proved at `d2975269`. No missing proof.

**Anti-patterns:** none reproduced. No `stub-as-truth` (stub filtered at provider, not persisted), no `silent tier upgrade` (exact-match `on_demand_*` required, `on_demand_runtime`/`authoritative_object_info` alias absent, `runtime_only` stays local-only without cloning) — per `batch-E.md:35` and `plan.md:43,191`.
0