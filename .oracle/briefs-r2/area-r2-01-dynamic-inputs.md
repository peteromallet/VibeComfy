Explore area (round 2): Dynamic-input contracts — which node classes legitimately grow inputs?

Context: the revised plan's B08-cut batch (deterministic endpoint integrity) fixes the C8/C9 root: resolve_source_endpoint falls back to the schema catalog when a source output name is absent from the working node's outputs; _apply_upsert_link writes origin_slot with no bounds check; projection resolves ports by index → "Missing stable link from port"; resolve_target_endpoint + _ensure_input_slot fabricate synthetic inputs for unknown names. B08 must NOT reject valid dynamic-port nodes.

Task: identify which node classes legitimately allow inputs/outputs to grow beyond the schema (dynamic/loose inputs: e.g., nodes with `input.required` as arbitrary lists, custom nodes, reroute/set/get proxies, nodes whose schema declares flexible port counts), and how the working node's real outputs array is populated vs the schema catalog. Report verified facts with file:line (projection_registry_v1.py, porting/resolution.py, apply_mutate.py, apply_resolve_add.py, apply_links.py, schema providers), the set of node classes that must be exempted from strict port checks, unknowns, risks, and the minimal invariant that distinguishes valid dynamic ports from C8 divergence. Ranked findings, <300 words.
